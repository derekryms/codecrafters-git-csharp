namespace codecrafters_git;

public static class Constants
{
    public const byte NullByte = 0;
    public const byte SpaceByte = 32;

    // public byte[] BlobTypeBytes = "blob"u8.ToArray();
    // public byte[] TreeTypeBytes = "tree"u8.ToArray();
    // public byte[] CommitTypeBytes = "commit"u8.ToArray();
    // public byte[] TagTypeBytes = "tag"u8.ToArray();
    //
    // public List<byte[]> SupportedObjectTypeBytes =
    // [
    //     "blob"u8.ToArray(),
    //     "tree"u8.ToArray(),
    //     "commit"u8.ToArray(),
    //     "tag"u8.ToArray()
    // ];
}